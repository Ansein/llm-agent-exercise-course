# 仓库协作规则

## 项目定位与范围

本仓库是一套中文「大模型与 Agent 企业实战」培训课程大纲。服务具备 Python 基础的工程师，覆盖传统 NLP、Transformer、BERT/GPT、模型构建与微调、RAG、多模态、Agent、Codex 与 Harness 工程。当前交付包含全课程教学设计与第01课可运行教学包；其他课的实验代码、数据和生产系统不得据此声称已经实现。

所有内容围绕课程设计与教学实施。用户最新要求优先；适用的系统与开发者约束保持有效。

## 目录与编号

- `README.md`：课程定位、课时、完整课次导航。
- `lessons/NN-主题.md`：一节课一个 Markdown 文件，使用连续两位课次编号。
- `projects/PXX-主题.md`：长图项目编号与项目任务书；P01—P10 永远对应原图项目一至十。
- `TODO.md`：短期完成项、当前进度与下一批实施计划。
- `CHANGELOG.md`：全生命周期实际交付更改，按时间倒序。
- `PROGRESS.md`：当前阶段成果汇报，每次完全重写。
- `ROADMAP.md`：长期稳定的阶段目标与推进路线。
- `docs/`：来源映射、教学实施、考核与技术参考；旧变更记录仅保留跳转入口。
- `templates/lesson-outline.md`：新课大纲模板；`templates/teaching-package.md`：完整教学包模板。
- `courseware/lesson-NN/`：按课次交付讲义、实验手册、数据、代码、Notebook、课件与参考输出；实际运行命令见该包README。
- 根目录原始 `.webp` 长图是来源证据，不覆盖、不压缩、不删除；裁剪和 OCR 中间文件不纳入正式内容。

课次是教学顺序，项目编号是来源顺序，两者不可混用。重排课程时同步更新 README、前后置依赖、项目映射及 CHANGELOG，避免孤立文件和失效链接。

## 项目管理文档维护

开始工作前阅读 TODO、PROGRESS 和 ROADMAP，确认短期任务、实际成果与阶段依赖。结束一段有实质进展的工作后，按以下边界维护，不能将同一工作流水复制进四份文档：

- **TODO**：记录近期已完成、当前状态和近期可执行计划。完成项仅保留最近14天、最多10项，旧项直接移除，不迁移成工作日志；未完成项保留、重排或明确取消。计划未开始时不能标为进行中或完成。
- **CHANGELOG**：只记录实际落地的课程内容、数据、代码、实验材料和可交付功能更改。最新日期置顶，同日新更改置于旧更改之前；保留全生命周期，不按TODO窗口裁剪。计划、讨论、测试执行以及TODO、PROGRESS、ROADMAP、CHANGELOG、仓库管理规则和导航等管理文档维护均不记录。测试发现并实际修复的问题可记录修复本身；无实质交付更改则不更新。
- **PROGRESS**：每次更新都完全重写为当前阶段成果快照，说明成果证据、成熟度、未交付内容和下一阶段目标；不追加历史段落，不把计划写成已交付，不按文件数量编造完成百分比。
- **ROADMAP**：只维护长期阶段、阶段目标、核心交付与阶段衔接条件。日常进度、完成标记、日期调整和细碎任务不写入；除非项目目标、范围或阶段依赖发生实质变化，尽量不修改。

TODO和PROGRESS注明实际更新日期。事实不明时先核对产物，不补造历史或性能结果。项目变更唯一入口为根目录 CHANGELOG.md，`docs/变更记录.md`仅保留兼容链接，不再维护第二份历史。

## 大纲内容标准

每节课必须包含：课程信息与先修条件、可观察的学习目标、业务痛点、分时教学安排、关键原理、传统与大模型方案对照、实验步骤、交付物、验收标准、课后练习、参考资料。默认 180 分钟，分段时长必须相加一致。

教学内容应具体到输入、处理过程、输出和失败处理，不用「掌握前沿技术」等口号替代目标。基础课落实到张量、损失、数据或推理实验；项目课落实到业务数据、接口契约与可测结果。

每个项目至少有一个非生成式大模型基线，并逐步比较预训练小模型、LLM、RAG 或 Agent 中适用的路线；无需为了堆技术强行加入全部路线。大模型不优于基线时也应允许合理结论。

## 企业项目与实验规范

- 明确角色、业务流程、样本规模、字段、权限、故障场景和交付边界。
- 数据按客户、文档族或时间分组切分；测试集冻结，不用测试标签调提示词或阈值。
- 性能指标必须注明分母、样本数、硬件、版本、并发、成本口径；百分比变化区分百分点与相对变化。
- 所有未运行指标标为「建议验收目标」或「待实测」，不得虚构完成状态、性能收益或行业领先结论。
- 业务成功率与安全失败单独统计；低频越权、重复写入和无法恢复等事件不能被平均分掩盖。
- 使用脱敏、授权或合成数据；合成数据用于教学，不声称等同真实生产分布。
- 默认连接沙箱、模拟 API 和只读数据库。发送外部消息、真实支付或生产写入应有明确授权；高风险动作在系统设计中具备审批、幂等及审计。
- 提供 CPU/离线缩小版与 GPU/API 完整版的边界；回放数据只能验证流程，不能替代模型质量评测。

## 技术准确性与时效

稳定原理引用原论文；变化较快的产品、协议、接口和兼容性引用官方文档，标注核验日期和适用版本。每期开课前复核 `docs/技术参考.md`，锁定实际模型 revision、依赖与协议版本。

「Harness Agent」在本课程中指具备运行控制、状态恢复、工具权限与评测机制的 Agent 系统，不声称是独立模型架构或统一产品名。Git worktree 用于工作副本隔离，不替代安全沙箱；提示缓存不等于长期记忆；RAG 不保证消除幻觉；反馈闭环不自动等于强化学习；Skills 共享不等于联邦学习。

区分 Agent Skills 通用规范与宿主扩展字段，不假定所有宿主支持自动安装依赖。分析公开行为与构建教学运行时，不宣称完整复刻闭源产品内部架构。避免要求模型输出隐藏思维链，用计划、工具轨迹、依据和结果解释开展教学。

## 修改与检查

使用 UTF-8 Markdown、中文正文和标准术语；首次出现术语给出中文释义。仓库内使用相对 Markdown 链接，便于迁移。提交前检查课次连续、所有项目有映射、所有课程有必需章节、180 分钟合计、内部链接可达、代码围栏闭合。

文档修改做结构与内容核验。第01课已提供可执行实验：安装命令见`courseware/lesson-01/README.md`，使用该环境Python执行`courseware/lesson-01/src/evaluate.py --output courseware/lesson-01/runs/<新目录>`；检查命令为`python -m unittest discover -s courseware/lesson-01/tests -v`。Notebook执行器、平台依赖及端口要求同样以教学包README为准。其他课新增代码时记录实际运行命令，不宣称通过不存在的测试。

冻结数据、配置及reference结果须保留版本与hash；实验输出写入新的runs目录，不覆盖基准。测试标签不得用于调整提示、规则或阈值。依赖、虚拟环境、构建中间文件不纳入正式交付。修改实现后同步讲义、答案与课件中的实际结果；技术复跑不等于第三方复现或学员试讲。

除非用户明确要求，不自动发布、发送消息、创建远程仓库或安装工具。已授权的文档编写与修订直接完成，不重复索要确认。
